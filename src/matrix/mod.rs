//! Matrix-side additions for the Chums client, kept outside the upstream tree.
//!
//! Currently only [`events`]: typed `cc.chums.*` custom events that the Chums client renders as
//! widgets instead of text bubbles.

pub mod events;
